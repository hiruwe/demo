pipeline {
    agent none 
    stages {
        stage('Pull') {	
            steps {
                sh 'mvn --version'
            }
        }, stage('build') {
        	steps {
                sh 'mvn --version'
            }
        },stage('Deploy') {
        	steps {
                sh 'mvn --version'
            }
        }
    }
}